// export const StatisticTitle = ({ title }) => {
//   return <h2 className="title">{title}</h2>;
// };

export const StatisticTitle = ({ title }) => {
  return <h2 className="title">{title}</h2>;
};
